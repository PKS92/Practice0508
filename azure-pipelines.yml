# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- none

pool:
  name: Agent0908

steps:
- task: TerraformTask@5
  inputs:
    provider: 'azurerm'
    command: 'init'
    backendServiceArm: 'prashantryansc2'
    backendAzureRmResourceGroupName: 'june25'
    backendAzureRmStorageAccountName: 'june25sa'
    backendAzureRmContainerName: 'june25container'
    backendAzureRmKey: 'ryankey.tfstate'

- task: TerraformTask@5
  inputs:
    provider: 'azurerm'
    command: 'validate'

- task: TerraformTask@5
  inputs:
    provider: 'azurerm'
    command: 'plan'
    environmentServiceNameAzureRM: 'prashantryansc2'
# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- none

pool:
  name: Pool1008

steps:
- task: TerraformTask@5
  inputs:
    provider: 'azurerm'
    command: 'init'
    backendServiceArm: 'RyanServiceConnection'
    backendAzureRmOverrideSubscriptionID: '5fbead97-423e-4814-809c-114db991d911'
    backendAzureRmResourceGroupName: 'PrashantRG'
    backendAzureRmStorageAccountName: 'prashantsaccount'
    backendAzureRmContainerName: 'prashant0908container'
    backendAzureRmKey: 'ryan.tfstate'

- task: TerraformTask@5
  inputs:
    provider: 'azurerm'
    command: 'validate'

- task: TerraformTask@5
  inputs:
    provider: 'azurerm'
    command: 'plan'
    environmentServiceNameAzureRM: 'RyanServiceConnection'
